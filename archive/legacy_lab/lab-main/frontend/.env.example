# ==============================================================================
# Frontend API Configuration
# ==============================================================================
# IMPORTANT: VITE_API_URL determines how the frontend connects to the backend
# ==============================================================================

# -----------------------------------------------------------------------------
# FOR LOCAL DEVELOPMENT (Vite dev server on http://localhost:5173)
# -----------------------------------------------------------------------------
# Use this when running `pnpm dev` locally with backend at http://localhost:8000
# This is the RECOMMENDED setting for local development
# IMPORTANT: Must include /api prefix to match Django URL structure
VITE_API_URL=http://localhost:8000/api

# -----------------------------------------------------------------------------
# FOR PRODUCTION (VPS at http://172.237.71.40 with Nginx)
# -----------------------------------------------------------------------------
# Use this for production deployment where nginx serves the frontend and
# proxies /api requests to the backend container
# Uncomment for production:
# VITE_API_URL=/api

# ==============================================================================
# Notes:
# - If VITE_API_URL is not set, the app will use mode-based defaults:
#   * development mode: http://localhost:8000/api
#   * production mode: /api
# - Always include /api prefix for development to match Django URL structure
# - Copy this file to .env and uncomment the appropriate setting
# - Never commit .env file to git (already in .gitignore)
# ==============================================================================
