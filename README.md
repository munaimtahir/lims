# Final Merged LIMS

This is the production-ready merged version combining:

- Repo B (`LIMS_new-main`) as the core architecture
- Repo A (`lab-main`) under `legacy_lab/` for full legacy features

All features are now staged for refinement into the modern app structure.
