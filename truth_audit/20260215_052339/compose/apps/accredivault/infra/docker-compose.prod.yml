services:
  db:
    image: postgres:16
    container_name: accredivault_db_prod
    environment:
      POSTGRES_DB: ${DB_NAME:-accredvault}
      POSTGRES_USER: ${DB_USER:-accredvault}
      POSTGRES_PASSWORD: ${DB_PASSWORD:-change_me}
    volumes:
      - PROD_accredivault_pgdata:/var/lib/postgresql/data
    restart: unless-stopped
    healthcheck:
      test: [ "CMD-SHELL", "pg_isready -U ${DB_USER:-accredvault} -d ${DB_NAME:-accredvault}" ]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - internal

  minio:
    image: minio/minio:latest
    container_name: accredivault_minio_prod
    command: server /data --console-address ":9001"
    environment:
      MINIO_ROOT_USER: ${MINIO_ACCESS_KEY:-minioadmin}
      MINIO_ROOT_PASSWORD: ${MINIO_SECRET_KEY:-minioadmin_change_me}
    volumes:
      - PROD_accredivault_minio_data:/data
    restart: unless-stopped
    healthcheck:
      test: [ "CMD", "curl", "-f", "http://localhost:9000/minio/health/live" ]
      interval: 15s
      timeout: 5s
      retries: 5
    networks:
      - internal

  backend:
    build:
      context: ../backend
      dockerfile: Dockerfile
    container_name: accredivault_backend_prod
    environment:
      DATABASE_URL: postgres://${DB_USER:-accredvault}:${DB_PASSWORD:-change_me}@db:5432/${DB_NAME:-accredvault}
      MINIO_ENDPOINT: http://minio:9000
      MINIO_ACCESS_KEY: ${MINIO_ACCESS_KEY:-minioadmin}
      MINIO_SECRET_KEY: ${MINIO_SECRET_KEY:-minioadmin_change_me}
      MINIO_BUCKET_EVIDENCE: ${MINIO_BUCKET_EVIDENCE:-evidence}
      MINIO_BUCKET_EXPORTS: ${MINIO_BUCKET_EXPORTS:-exports}
      SECRET_KEY: ${SECRET_KEY:-django-insecure-change-in-production}
      DEBUG: ${DEBUG:-False}
      ALLOWED_HOSTS: ${ALLOWED_HOSTS:-phc.alshifalab.pk,sos.alshifalab.pk,api.phc.alshifalab.pk,localhost,127.0.0.1,backend}
      CORS_ALLOWED_ORIGINS: ${CORS_ALLOWED_ORIGINS:-https://phc.alshifalab.pk,https://sos.alshifalab.pk}
      CSRF_TRUSTED_ORIGINS: ${CSRF_TRUSTED_ORIGINS:-https://phc.alshifalab.pk,https://sos.alshifalab.pk,https://api.phc.alshifalab.pk}
    volumes:
      - ../backend:/app
    depends_on:
      db:
        condition: service_healthy
      minio:
        condition: service_healthy
    restart: unless-stopped
    healthcheck:
      test: [ "CMD", "python", "-c", "from urllib.request import urlopen; urlopen('http://localhost:8000/api/v1/health')" ]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 30s
    networks:
      - internal

  compliance_cron:
    build:
      context: ../backend
      dockerfile: Dockerfile
    container_name: accredivault_compliance_cron_prod
    environment:
      DATABASE_URL: postgres://${DB_USER:-accredvault}:${DB_PASSWORD:-change_me}@db:5432/${DB_NAME:-accredvault}
      MINIO_ENDPOINT: http://minio:9000
      MINIO_ACCESS_KEY: ${MINIO_ACCESS_KEY:-minioadmin}
      MINIO_SECRET_KEY: ${MINIO_SECRET_KEY:-minioadmin_change_me}
      MINIO_BUCKET_EVIDENCE: ${MINIO_BUCKET_EVIDENCE:-evidence}
      MINIO_BUCKET_EXPORTS: ${MINIO_BUCKET_EXPORTS:-exports}
      SECRET_KEY: ${SECRET_KEY:-django-insecure-change-in-production}
      DEBUG: ${DEBUG:-False}
    command: >
      sh -c "
        mkdir -p /etc/crontabs /app/logs &&
        cp /app/scripts/cron/root /etc/crontabs/root &&
        busybox crond -f -l 8 -c /etc/crontabs
      "
    volumes:
      - ../backend:/app
    depends_on:
      db:
        condition: service_healthy
      minio:
        condition: service_healthy
    restart: unless-stopped
    networks:
      - internal

  frontend:
    build:
      context: ../frontend
      dockerfile: Dockerfile
      args:
        VITE_API_BASE_URL: ${VITE_API_BASE_URL:-https://phc.alshifalab.pk/api/v1}
    container_name: accredivault_frontend_prod
    environment:
      VITE_API_BASE_URL: ${VITE_API_BASE_URL:-https://phc.alshifalab.pk/api/v1}
    volumes:
      - ../frontend:/app
      - /app/node_modules
    restart: unless-stopped
    depends_on:
      backend:
        condition: service_healthy
    healthcheck:
      test: [ "CMD", "node", "-e", "require('http').get('http://localhost:5173', (r) => process.exit(r.statusCode === 200 ? 0 : 1)).on('error', () => process.exit(1))" ]
      interval: 30s
      timeout: 10s
      retries: 5
    networks:
      - internal

  caddy:
    image: caddy:2
    container_name: accredivault_proxy_prod
    ports:
      - "127.0.0.1:8016:80"
    volumes:
      - ./Caddyfile:/etc/caddy/Caddyfile:ro
      - PROD_accredivault_caddy_data:/data
      - PROD_accredivault_caddy_config:/config
    depends_on:
      backend:
        condition: service_healthy
      frontend:
        condition: service_healthy
    restart: unless-stopped
    healthcheck:
      test: [ "CMD", "wget", "--quiet", "--tries=1", "--spider", "http://localhost/health" ]
      interval: 30s
      timeout: 10s
      retries: 5
    networks:
      - internal

networks:
  internal:
    name: prod_accredivault_network

volumes:
  PROD_accredivault_pgdata:
    name: PROD_accredivault_pgdata
  PROD_accredivault_minio_data:
    name: PROD_accredivault_minio_data
  PROD_accredivault_caddy_data:
    name: PROD_accredivault_caddy_data
  PROD_accredivault_caddy_config:
    name: PROD_accredivault_caddy_config
