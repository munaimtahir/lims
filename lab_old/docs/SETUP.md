# Setup (Dev)
1. Copy `infra/.env.example` to `.env` in repo root and fill values.
2. Ensure Docker Desktop is running.
3. `docker compose -f infra/docker-compose.yml up -d --build`
4. Backends and frontends will be added in later stages via AI prompts.
